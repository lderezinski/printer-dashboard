{
  "name": "flashforge-health",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "static:render": "node scripts/render-static-site.mjs",
    "static:watch": "node scripts/render-static-site.mjs --watch",
    "static:serve": "node scripts/serve-static-site.mjs",
    "test": "node --test",
    "security:check": "node scripts/security-check.mjs",
    "security:hooks": "git config --local core.hooksPath .githooks"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ipp": "^1.1.0",
    "mqtt": "^5.15.2",
    "net-snmp": "^3.29.1"
  },
  "license": "AGPL-3.0-only"
}
